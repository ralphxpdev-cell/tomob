# TOMOB Studio WORK 작업일지

> 이전 로그: `work-archive/older.md` 참조 (2026-07-28 이전)
> append-only. 과거는 전부 여기. `_PROJECT.md`는 지금 상태만 덮어쓴다.

---

## 2026-08-13

### 회사소개서 케이스 스터디 중심 재구성 (24p) — /plan 컨펌 후 진행

- [완료] **전 프로젝트 폴더 전수 파악** — `_PROJECT.md` 44개 확인. 실측 수치가 있는 자동화 사례 7건 발굴: 이커머스(114개 라이브·315장·132초), PEACH(13,023건·1,916건), tomob-seed(접수→DB+텔레그램), BizRadar(매일 09시 공고 자동 발송), TOMOB AX 봇(조회 명령 7종), SeedLog(현장 기록→리포트), LIME v1(수집→시안 생성→콜드메일)
- [완료] **AX 시장 리서치** — 처음AX·AX파트너·AX-Consulting 3사 서비스 항목 실측(설문 보고서 5일→1시간, 나라장터 모니터링, 예약 노쇼 40% 감소 등). What we change를 일반론 3건에서 **실제 AX 업무 6영역**으로 재작성, 각 영역에 자사 케이스 번호 연결
- [완료] **구조 재편 24페이지** — 프로젝트 11건 → PEACH·LIME 2건(각 1p, "피치 피치 라임 라임" 반복 제거). 목업은 브랜드 톤 배경 패널+정면 창 겹침 컴포지션으로 재제작(compoPeach 핑크/compoLime 라임). Case Studies 챕터 신설(7건, 1건 1p: 지금까지의 방식→구축한 것→실측 수치 3개→자사 운영 명기). 화면 없는 케이스(공고·협업·아웃리치)는 파이프라인 다이어그램
- [완료] 24페이지 전수 인쇄 검증(헤드리스 PDF→pdfium 렌더). 산출물 `영업/TOMOB-AX-회사소개서-2026-08-12.html` + `.pdf`(6.1MB)

### 케이스 스터디 비주얼 전면 재제작 (22p) — /plan 컨펌 후 진행

- [완료] **CASE 1(커머스)·2(리드) 삭제** — Products(PEACH·LIME)와 화면 중복. 케이스 5건으로 재번호, 실측 수치(114개·315장·132초 / 13,023건·1,916건)는 Products 페이지 수치 칩으로 이관. What we change 연결 라벨 6곳 재배선
- [완료] **접수 케이스 화면 교체** — 구버전 다크 폼 캡처("저거 아님" 판정)를 버리고 현재 라이브 `tomob-seed.vercel.app/request`(밝은 AX 5단계 접수)를 헤드리스로 새로 캡처
- [완료] **콘셉트 목업 3장 Codex(GPT image_gen) 신규 생성** — 공고 모니터링(모니터+폰 알림)·협업 보고(워크로그→봇 요약)·아웃리치(리드→제안 사이트→메일 승인). 블루 톤 통일, `WORKFLOW CONCEPT · 실제 화면 아님` 고지 태그. `$null | codex exec --full-auto` 헤드리스, 장당 1~2분
- [완료] **현장 기록 케이스** — 실제 모바일 뷰포트 캡처(mobile-home 원본)로 교체, 크롭 없는 온전한 폰 프레임
- [완료] 22페이지 전수 인쇄 검증. 산출물 `.html` + `.pdf`(9.4MB). 검사기 지적 1건은 랜딩 확정 카피 원문 유지

### 콘셉트 목업 한글화 (3장)

- [완료] **콘셉트 목업 3장 한글 UI로 재생성** — 사령관 판정 "목업이 영어네, 한글로 다시". `$null | codex exec --full-auto`(image_gen), 화면 문구를 한 줄씩 지정한 프롬프트로 생성. 한글 렌더는 세 장 모두 또렷하게 나왔다
- [완료] **글자 오류 2건 화소 수정** — 아웃리치 우상단 배지가 「정상 작동 증」으로, 협업 보고 1번 카드 이름이 「잔성은」으로 나왔다. 재생성하면 다른 곳이 깨질 수 있어 해당 영역만 고쳤다. 배지는 맑은 고딕으로 다시 그리고(그레이스케일 안티에일리어싱), 이름은 같은 이미지 3번 카드의 「전성은」 화소를 375px 위로 복사
- [완료] **협업 보고 인물 처리 변경** — 1차 생성본에 AI가 만든 사람 얼굴 사진이 전성은·유경탁 실명과 함께 들어갔다. 고객에게 나가는 문서라 실제 인물로 오해될 수 있어 이름 첫 글자 원형 아바타로 재생성
- [완료] 영문 원본 3장은 `_소개서_assets/_구버전영문/`으로 보존. 아웃리치 이미지 비율이 1717:916 → 3:2로 바뀌어 HTML의 `aspect-ratio` 인라인 지정 제거
- [완료] **재인쇄·검증** — 포트 8931 서빙 → 헤드리스 크롬 print-to-pdf → pypdfium2로 15·16·18쪽(CASE 02·03·05) 렌더 대조. 22페이지 유지, 잘림 없음. 산출물 `.pdf`(9.6MB)

### 콘셉트 그림 → 실제 화면 교체 (사령관 지시)

- [완료] **CASE 02 공고 모니터링을 실제 화면으로 교체** — 사령관 "땅콩봇 폴더 가서 목업 보고 만들면 될 것 같고". `ddangkongbot/bizradar/web.py`의 대시보드 템플릿 원문에 기업지원플러스 공개 API의 **당일 실제 공고**를 넣어 렌더하고 헤드리스로 캡처했다(수신 100건 → 키워드 통과 73건). 콘셉트 그림과 「실제 화면 아님」 태그를 걷어내고 CASE 01과 같은 브라우저 프레임으로 교체
  - 배포본은 못 쓴다: `ddangkongbot.vercel.app`은 500이고 `bizradar.vercel.app`은 지금 다른 서비스(사업성 진단)로 바뀌어 있다. `.env`의 Supabase 프로젝트(`rgbfuctm…`)는 호스트가 사라졌다
  - ⚠️ 남은 결함: `web.py`의 `_dday()`는 8자리 숫자만 파싱하는데 API는 `2026-08-11 ~ 2026-08-17` 형식을 준다. 배포본이 살아나도 D-day가 전부 「기간 미상」으로 나온다. 이번 캡처는 렌더 스크립트에서만 보정했고 **저장소는 손대지 않았다**
  - 크롭 주의: `.bBody`가 `object-fit:cover`라 가로가 잘린다. 1500×1000으로 찍었더니 제목이 잘려 상자 비율(118:142)에 맞춰 1250×1500으로 다시 찍었다
- [완료] **LIME 상세페이지 에디터 화면 추가** — 사령관 "라임도 상세페이지 에디터하는 부분 목업 사진이 빠짐". `lime-editor.png`가 에셋에 있는데 HTML에서 참조되지 않고 있었다. 아래 3할이 빈 화면이라 70%로 잘라 `lime-editor-crop.png`로 넣었다. 대시보드를 빼지 않고 **창 4개 배치로 재조정**(콘솔 top:15mm·46% / 대시보드 좌하 30%·34% / 에디터 중하 44%·42% / 생성 결과 우 26%·84%). 콘솔을 15mm까지 내려야 「LIME · 실제 운영 화면」 태그와 겹치지 않는다
- [완료] **CASE 03·05 콘셉트 그림을 직접 만든 UI로 교체** — 사령관 "다른 케이스도 UI만 네가 만들어서 목업으로 하면 되잖아". AI 그림은 글자가 깨져 사후 보정이 들지만 HTML은 안 깨진다
  - 원본: `_소개서_assets/_mockup/case-collab.html`·`case-outreach.html`. 1536×1024 헤드리스 캡처. 재생성 방법은 각 파일 상단 주석에 있다
  - **내용을 지어내지 않았다.** 협업 보고의 기록 7건은 `sungeunstar/tomob-ax` 실제 커밋(197e3e8·aa94bac·6bd2f6d·c39b90c·1d8aede·951531e·3081a6f)이고, 봇 응답은 `api/telegram-webhook.mjs`의 `helpText()` 출력 원문이다. 아웃리치의 메일 본문은 `03_아웃리치/260806_메시지_템플릿팩.md` E-1 원문이다
  - 세로가 남는 문제: 아웃리치는 1138×759로 짜고 `zoom:1.35`로 확대해 3:2 판을 채웠다. 협업 보고는 기록 행을 7건으로 늘려 채웠다
  - 고지 태그를 한글로 교체 — `WORKFLOW CONCEPT · 실제 화면 아님` → `화면 예시 · 실제 서비스 화면 아님`
  - AI 생성본 3장은 `_소개서_assets/_구버전AI/`에 보존. PDF 9.6MB → 6.0MB
- [완료] **LIME 상세페이지 에디터 — 실제 화면으로 교체** — 사령관 "저거 아니라니까. 실제 이미지 교체랑 텍스트 교체하는 화면 있다고". 앞서 넣은 `lime-editor.png`는 상품정보 입력 폼이었고, 진짜는 `project/이커머스`의 캔버스 에디터다
  - `newseason/assets/lime/app-editor-canvas.png`는 빈 캔버스라 못 쓴다. 실제로 띄워서 찍었다
  - ⚠️ **node_modules가 맥에서 설치돼 있어 윈도우에서 안 뜬다**(`better_sqlite3.node` 매직바이트 `CF FA ED FE` = Mach-O, 에러 193). 사령관 폴더를 건드리지 않으려고 `api`·`connectors`·`app`을 스크래치패드로 복사해 거기서 `npm install`했고, Node 24는 better-sqlite3 프리빌드가 없어 **Node 22 포터블**을 따로 받아 구동했다
  - ⚠️ API의 CORS가 `localhost:3000`만 허용한다. 다른 포트로 띄우면 화면이 `Failed to fetch`로 빈다
  - 레이아웃이 저장된 상품은 DB에 **1건뿐**(`prod-58d5c6df…` 차량용 무선 미니 청소기). `Product.detail_layout_json`으로 찾는다
  - 캔버스에 줌 기능이 없어 뷰포트를 1500×1900으로 키워 문서를 더 보이게 하고, playwright로 「15,900원」 텍스트를 선택해 옵션 패널(타이포그래피·채우기·텍스트 그림자)이 열린 상태로 찍었다. 상단 1150px만 잘라 `lime-editor-live.png`, 전체는 `lime-editor-full.png`
  - LIME 페이지 창 배치를 재조정 — 에디터를 가장 큰 창으로 올리고 운영 콘솔을 중간 창으로 내렸다
- [반려·되돌림] **아래가 비던 6개 장 여백 정리 시도** — 5·7·8·9·20·21쪽. `.page.stretch` 클래스로 카드를 세로로 늘려(번호 위/본문 아래 `margin-top:auto`) 판을 채우려 했으나, 사령관 "박스만 키우니까 더 이상해 보이는데 내용이 비어 보이고" — 상자가 커지자 안이 빈 게 더 도드라졌다. 전량 되돌렸다(`stretch` 클래스·관련 CSS 전부 제거)
  - 원인 진단: 세로로 긴 A4 3분할 상자에 3줄짜리 글이 들어가는 조합 자체가 안 맞는다. 상자를 늘리면 속이 비고, 안 늘리면 아래가 빈다
  - 다음 시도 방향(미착수, 사령관 검토 대기): 상자를 눕혀 가로 띠 3줄로 바꾸거나(내용 안 바꾸고 판형만 교체), 원칙마다 실제 사례 한 줄을 추가해 분량으로 채운다
- [완료] **마지막 장에 운영 주체 표기** — 사령관이 값을 줌. Brimming Studio · 서울특별시 노원구 덕릉로 70가길 101 · sungunbyeol@gmail.com · 영문 태그라인. 표지 하단과 마지막 장 저작권 표기도 브리밍으로 통일했다. 기존 `tomobstudio@gmail.com`은 전 문서에서 뺐다(메일 주소가 두 개면 혼란)
  - 사령관이 준 원문의 오타 2건을 고쳐 넣었다 — `Coppyright` → `Copyright`, `All Right Reserved` → `All Rights Reserved`
- [완료] **고지 태그 제거** — 사령관 "화면 예시 · 실제 서비스 화면 아님 이건 빼고". CASE 03·05 두 곳
- [완료] **땅콩봇 날짜 파싱 결함 수정** — `ddangkongbot/bizradar/web.py`의 `_dday()`·`_format_date()`가 8자리 숫자만 찾아 API의 `2026-08-11 ~ 2026-08-17` 형식을 못 읽었다. 정규식을 `\d{4}-?\d{2}-?\d{2}`로 바꾸고 두 형식 모두 통과하는 것을 확인했다(D-4 / D-35 / 마감 / 기간 미상)

### SEO — OG 태그·이미지·파비콘·robots·sitemap (배포 전)

- 발단: 사령관 "카톡공유했는데 이미지도 별로고" — `/ax` 링크를 카톡으로 공유했을 때 썸네일이 부실했다는 지적
- [완료] **원인 확인** — `app/layout.tsx`·`app/ax/page.tsx` 어디에도 `openGraph`·`twitter` 메타데이터가 없었다. `www.tomob.cloud/ax`를 직접 curl해 head를 봐도 `og:` 태그가 0개, favicon 링크도 없었다
- [완료] **OG 이미지 2장 신규 제작** — 메인용 `public/og-image.png`, `/ax`용 `public/ax/og-image.png`(1200×630). 실제 코드 색상 토큰(`--accent:#1d4ed8`·`--navy:#0b1322`)을 그대로 써서 HTML로 짜고 헤드리스로 캡처. 카피는 각 페이지 확정 title·description 그대로, 새로 짓지 않았다
- [완료] **파비콘 신규 제작** — `app/icon.png`(512×512, 남색 배경+흰 "T"). 기존 `public/logowhite.png`는 흰 글자만 있어 파비콘으로 못 쓴다(투명 배경 위 흰색이라 안 보임)
- [완료] `app/layout.tsx`에 `metadataBase`·`openGraph`·`twitter`(summary_large_image) 추가. `app/ax/page.tsx`에도 페이지 전용 OG·트위터카드 추가. 네이버/구글 소유확인 코드 넣을 자리를 주석으로 미리 만들어 둠(`verification` 필드)
- [완료] `app/robots.ts`·`app/sitemap.ts` 신설(Next.js App Router 컨벤션). 사이트맵은 `/`·`/ax` 2건 — 현재 라우트가 이 둘뿐
- [완료] **로컬 dev 서버(포트 3222)로 전부 실측 검증** — og:image URL, og:title, twitter:card, favicon link, robots.txt 본문, sitemap.xml 200 전부 확인 후 서버 종료. `tsc --noEmit` 통과
- ⚠️ **배포 안 함(작성 시점).** 외부 배포는 컨펌 필요 항목이라 여기서 멈췄다 — 2026-08-14 세션에서 배포 완료
- 🔴 **도메인 소유 재확인** — `vercel domains ls`로 이 계정(`junsungeuns-projects`)을 보면 `tomob.cloud`가 없고 `jandhgroup.co.kr` 하나뿐이다. 그런데 `www.tomob.cloud/ax`는 지금 200에 정상 AX 콘텐츠를 낸다. 즉 **이 계정이 배포하는 `tomob-studio.vercel.app`과 실제로 살아 있는 `www.tomob.cloud`는 서로 다른 배포**다. 오늘 만든 OG·robots·sitemap을 `tomob-studio.vercel.app`에 배포해도 `www.tomob.cloud`에는 안 나간다 — 그 도메인을 서빙하는 다른 계정/프로젝트를 찾아야 한다(`_PROJECT.md` §지금 막고 있는 것에 예전부터 있던 이슈, 재확인만 함)
- 남은 일: (1) 네이버 서치어드바이저·구글 서치콘솔 — 둘 다 사령관 계정 로그인 필요, 소유확인 코드를 받으면 `layout.tsx`의 `verification` 주석 자리에 한 줄씩 넣으면 끝

---

## 2026-08-12

### AX 회사소개서 제작 — 랜딩 기반 A4 가로 11페이지

- [완료] **`영업/TOMOB-AX-회사소개서-2026-08-12.html` 신규 작성** — 브라우저 인쇄로 PDF 저장하는 A4 가로 문서. 표지 → ABOUT → 진단 체크리스트 → WHAT WE CHANGE(Before/After 3건) → SERVICES(FIND·BUILD·RUN) → FLOW(4단계) → WORK(PEACH·LIME) → OWNERSHIP → ENGAGEMENTS(가격 3단계) → FAQ 6문항 → CONTACT
  - 카피는 전부 `app/ax/page.tsx` 확정 카피 원문. 새로 짓지 않았다
  - 디자인은 랜딩 실측 토큰 그대로 — SaaS 블루 `#1D4ED8` · 다크 `#0B1322` · 면 교대 `#FFF`↔`#F7F9FC` · 1px `#E6EBF2` · Pretendard
  - 이미지는 기존 실물 에셋만 사용 — 표지·마지막 페이지 `hero-field.png`, WORK `card-peach.png`·`card-lime.png`. 신규 GPT 생성 필요 없음
  - 검증: 로컬 서버로 11페이지 전부 실렌더 확인. 문체 검사기 지적 2건은 랜딩 확정 카피 원문이라 유지
  - 레퍼런스 폴더(에이전시_회사소개서) PDF는 위에이알 1부 구조만 추출해 참조 — 표지→회사→서비스→실적→문의 표준 구성 확인

### 회사소개서 전면 재구성 — 랜딩 문법 → 소개서 문법

- [완료] **"회사소개서보다 랜딩페이지 같다" 판정 반영** — 1차본은 랜딩의 설득 흐름(체크리스트→가격→FAQ→CTA)을 그대로 옮긴 것이 원인. 자사 기존 소개서 `브리밍2024.pdf`를 페이지 이미지로 렌더해 구조를 실측하고 그 문법으로 재구성
  - 브리밍 구조: 표지 → CONTENT 목차 → Introduction(한 문장 정의) → What we do → About(연혁) → Project 1건 1페이지(제목·유형·Client·Description·실물 목업) → Contact. 가격·체크리스트·FAQ 없음
- [완료] **8페이지 재작성** — 표지 / CONTENT / Introduction / What we do(FIND·BUILD·RUN+도구) / Project PEACH / Project LIME(mockup-*.png 사용) / Process(FLOW 4단계+전담팀 문구) / Contact. 디자인 토큰은 유지(사령관 "디자인은 괜찮다")
  - 삭제: 진단 체크리스트 · Before/After · ENGAGEMENTS 가격표 · FAQ (기존 세일즈키트 `tomob-ax/04_세일즈키트` 소개서가 담당)
- [완료] **PDF 동시 산출** — 헤드리스 크롬 print-to-pdf로 `영업/TOMOB-AX-회사소개서-2026-08-12.pdf` 생성, pdfium 렌더로 8페이지 전부 인쇄 기준 검증. 프로젝트 페이지 목업이 푸터 침범하던 것 수정
- 잔여: 연혁(About) 페이지는 지어낼 수 없어 미수록. 브리밍 시절 실적(라이킨·썸무드·라프코) 수록 여부는 사령관 결정 대기

### 회사소개서 28페이지 확장 — 정면 실제 화면 + 프로젝트 11건

- [완료] **목업 전면 교체** — 사령관 "옆에서 본 목업은 하나도 안 보인다" 판정. 기울어진 mockup-*.png을 버리고 `project/newseason/assets`의 정면 실제 화면(사령관 안내)을 CSS 브라우저 프레임(정면)에 넣는 방식으로 교체. 사용 화면은 `영업/_소개서_assets/`에 복사·최적화(2000px·jpg 82)
- [완료] **12p → 28p 확장** — 사령관 "다른 회사는 거의 30페이지" 지시. 챕터 디바이더 4장 + 원칙 페이지 + 우선 진단 업무 3건(1건 1페이지) + 프로젝트 11건: 자사 8(PEACH 2p·LIME 2p·SEED·SEEDLOG·스타딜리버리·TOMOB GAME·FLOWING·토핑업) + 클라이언트 3(LAIKIN·SOMEMOOD·LAFCO, "브리밍 스튜디오 수행" 명기·브리밍2024 설명 원문 사용)
- [완료] **크롭 결함 3건 수정 후 28페이지 전수 인쇄 검증** — LIME 에디터 빈 화면→데이터 화면 교체, SEED 세로 과다 크롭→와이드 프레임, SEEDLOG 폰 프레임 크롭 어긋남→컬럼 픽셀 실측 크롭+프레임 비율을 이미지에 맞춤(aspect-ratio)
- 산출물: `영업/TOMOB-AX-회사소개서-2026-08-12.html` + 동명 `.pdf`(7.9MB, 28p)
- 잔여: 연혁(About us 타임라인)은 사령관 자료 대기. 문체 검사기 지적 1건은 랜딩 확정 카피 원문이라 유지

---

## 2026-08-11

### `/ax` 전면 재작업 — 에디토리얼 → 기업 사이트

- [완료] **레퍼런스 3사 실측 분석** — 사령관이 `litmers.com` · `ax.cheomservice.com` · `joshua.site` 지정. `getComputedStyle`로 폰트·컬럼·타입스케일·면·선·라운드·그림자 전수 측정해 `영업/AX-레퍼런스-실측분석-2026-08-11.md` 작성. 3사 전부 Pretendard라 AGR의 Inter 충돌이 사라짐. 콘텐츠 폭 1146/1200/1440
  - **왜:** 사령관 "너무 디자인 에이전시 같아서 조금 더 전문적인 느낌으로". 1차에 정보 구조 문제로 오독했다가 "구조를 보라고 한 게 아니라"로 정정받고 디자인 언어만 재실측
  - 결론: "전문성"은 비주얼 기법이 아니라 **면 + 1px 테두리 + 카드**의 문제였다. 시각적으로 가장 화려한 litmers가 가장 덜 전문적으로 읽히고, 장식이 거의 없는 처음AX가 가장 전문적으로 읽힌다

- [완료] **디자인 토큰 전면 교체** — 사령관 컨펌 "처음AX형 SaaS 블루". 액센트 `#ED441A`→`#1D4ED8`, 버튼 알약 100px/12px→radius 14px/15px, 카드 테두리 0개→1px `#E6EBF2`, 그림자 0개→2단 소프트, 전면 화이트→`#FFF`↔`#F7F9FC`↔다크 `#101E33` 교대, 90px 대문자 영문 제목→12px 영문 kicker + 한글 34px, 섹션 헤어라인 제거(3사 모두 선을 긋지 않음)
  - 액센트는 처음AX 실측 `#2B5CD6`을 그대로 쓰지 않고 한 단계 진한 값으로. 경쟁사 브랜드색 회피 — 사령관에게 보고함

- [완료] **히어로 교체 3차** — 처음AX형 다크 2단 + 목업 카드 → joshua형 밝은 중앙 → **최종: 풀페이지 WebGL 성운 + 마우스 유체**. 사령관 "우리가 만들었던 그 불꽃을 블루/우주 계열로 바꾸고 마우스 인터랙션". 불꽃 텍스처 워드마크는 내비 20px 플랫 텍스트로 내림
- [완료] **마우스 인터랙션 재작성** — 1차는 커서 주변 도메인을 회전시키는 **가짜 왜곡**이라 "예전에 그 느낌이 아닌데"로 반려. `HeroVisual.tsx`의 실제 Navier-Stokes 풀이(이류→와도→와도강화→발산→압력 자코비 20회→그래디언트 차감)를 상수까지 그대로 이식하고, 미는 대상만 은하 별 → 불꽃 fBm 무늬로 교체. SIM 128 / DYE 512 / curl 2.2 / 속도소산 1.6 / 염료소산 0.85
  - **왜:** 예전 구현을 안 읽고 새로 짠 것이 원인. 정본 주석에 "휘젓는다는 건 왜곡이 아니라 저은 자국이 남는 것"이라고 근거까지 적혀 있었다
  - 검증: 커서 스윕 후 자국이 곡선으로 남고 4초 뒤 소용돌이치며 잦아드는 것 확인
- [완료] "커서를 움직여 보세요" 안내 문구 제거 (사령관 지시)

- [완료] **카피 전면 교체** — 사령관 "전체적인 카피들이 너무 거지같아". 원인은 확정 기획안(`영업/TOMOB-AX-랜딩페이지-기획안-2026-08-06.md`)이 있는데 안 읽고 밋밋한 문장을 지어낸 것. 기획안 §4 원문 + 레퍼런스 3사 문법(대조 구문 / 부정 선언 / 동사 종결)으로 재작성
  - 히어로 "사람을 더 뽑기 전에, 일이 새는 구조부터 바꿉니다" · WORK "말보다, 실제로 만든 것으로 설명합니다" · WHY TOMOB "자동화할 수 있다고, 모두 자동화하지 않습니다" · SERVICES "진단하고, 만들고, 운영합니다" · FAQ 6문항 기획안 원문
  - CTA 전 페이지 "무료 30분 점검" 단일화

- [완료] **WORK 카드 newseason 원본 규격으로 복구** — gap 20→10px, radius 20→8px, 이미지를 카드 안 일부 → **풀블리드 + 텍스트 오버레이**, 좌상단 태그 pill, hover 시 이미지 1.05 확대. 밝은 썸네일(PEACH·LIME)은 원본 `card-ai-tag--light`와 같이 태그 반전 + 하단 스크림 강화
- [완료] **WORK를 PEACH·LIME 2개로 축소** — 사령관 "피치, 라임만 일단. 프로덕트 자체 가지고 있는 거?". 8개를 다 걸면 소개팅 앱·웹게임·커리어 앱이 "반복 업무 자동화"의 증거가 아니라 **AX를 안 해봤다는 인상**을 준다. 나머지 6개는 `allProjects`에 보존하고 `SHOWN` 배열로 제어

- [완료] **설득 구조 4종 신설** — 사령관 "난 저 흐름이 전혀 설득이 안 되는데 넌 설득이 됨?" → 나도 안 된다고 답하고 진단 후 착수
  1. 자가 진단 체크리스트(`PainCheck.tsx`) — 기획안 §4.3 6항목, 3개 이상 시 판정 문구 전환
  2. Before/After 3종 — 기획안 §4.4 원문 + 측정할 값. "구축 완료 사례 아님" 명시
  3. FLOW를 단계별 [TOMOB / 고객 / 산출물 / 다음 단계 조건]으로 분리 — SERVICES와의 중복 해소
  4. 가격 3단 — 무료(30분) / 200만 원부터(2–4주) / 협의(월 단위). 기획안 §4.5 원칙대로 "업무 분석 문서까지 무료로 드리지 않습니다" 병기
- [완료] IN NUMBERS 문구 교체 — "아직 고객사 수치는 없습니다"(자기비하) → "첫 파일럿의 도입 전후 수치는 측정 기준과 산식을 함께 공개합니다. 근거 없는 절감률은 쓰지 않습니다"

- [완료] **THE TEAM → OWNERSHIP 재서술** — 사령관 "이 부분은 마이너스 아닌가" + "회사처럼 보여야지". 인원 수 화법을 전부 제거하고 사령관 지정 문구 "회사마다 전담팀이 배정되어 끝까지 책임집니다"로 교체, 뒤에 지속성 답변(운영 매뉴얼·관리 화면을 회사에 남겨 언제든 이어받을 수 있음) 추가
  - **왜:** litmers는 규모를 신뢰로 쓰고(100+), joshua는 실적을 깔고 나서 "한 팀"을 쓰며, 처음AX는 팀 얘기를 아예 안 한다. TOMOB는 실적이 없어 인원을 광고하면 마이너스
  - 수치도 교체: `2 프로덕트`/`2 책임 파트너`/`2–4주` → `8 직접 설계·구축`/`30분 무료 점검`/`2–4 첫 파일럿(주)`
  - ⚠️ "전담팀"이 팀 여러 개로 읽힐 수 있다는 점은 사령관에게 보고했고 그대로 진행 결정
- [완료] **인수인계 트랙 추가** — 사령관 "여기 아래 뭔가 요소가 있어야 할 것 같은데? 인터랙션이라던가". 흔한 진행(영업─╳─기획─╳─제작─╳─운영, 점선+끊김 표시) vs TOMOB(실선, 점이 7초에 걸쳐 끝까지 이동). 새 JS 없이 `Reveal`의 `.isIn`으로 뷰포트 진입 시에만 구동
  - ⚠️ 2026-08-14: 이 인터랙션은 레퍼런스에 없는 즉흥 장치였고 세 번을 고쳐도 어색함이 안 잡혀 결국 제거함 — 상세는 2026-08-14 로그(다음 세션 자동 기록) 참조

### 버그 · 성능

- [완료] **앵커 id 충돌** — `/ax`에 `id="pain"`을 썼더니 섹션이 통째로 검게 깔림. 원인은 `globals.css:161` `#pain{background:var(--black)}`. `#hero` `#portfolio` `#service` `#process` `#cta`도 전역으로 잡혀 있어 `/ax` 앵커 전부에 `ax-` 접두어 부여
- [완료] **첫 페인트 9.8초 수정** — 사령관 "로딩이 조금 있네? 로딩창을 만들어야 하지 않나". 측정 결과 TTFB 17ms / load 1.6초인데 first-paint 9,856ms. 원인은 **메인 사이트 로딩창이 `/ax`에 깔린 채 안 걷힌 것** — `Intro.tsx`가 `#page`를 못 찾아 early return하고 5초 fallback 타이머로만 사라졌다. `Intro`·`Cursor`·`ClientEffects`에 `usePathname` 가드를 넣어 `/ax`에서 렌더 안 함 + `#page` 없는 라우트에서도 오버레이를 걷도록 원 버그도 수정
  - 렌더 블로킹 스타일시트 5→3개 (Geist·Instrument Serif·Nanum Myeongjo 참조 0건 확인 후 제거)
  - **왜:** 로딩창을 더 얹었으면 원인은 그대로 두고 가리기만 했을 것

### 배포

- [완료] **`tomob-studio` Vercel 프로젝트 신규 생성 + 프로덕션 배포** — `junsungeuns-projects` / `tomob-studio.vercel.app`. 사령관 선택 "새 프로젝트 프로덕션 / 커밋 없이". `.vercelignore` 신설(`image/` 85MB·`영업/`·`sessions/`·`*.psd` 제외, 참조 0건 확인)
- [확인필요] **`www.tomob.cloud/ax`가 404** — 루트는 200(Vercel). 실제 회사 도메인은 내가 배포한 프로젝트가 아니라 **다른 Vercel 프로젝트**에서 나가고 있고, CLI 계정에서는 안 보인다
- [확인필요] GitHub 연결 실패 — `ralphxpdev-cell/tomob`에 이 Vercel 계정 권한 없음(400). 자동 배포 불가, 매번 CLI 수동 배포
- [확인필요] **`/ax` 전체가 아직 untracked** — 로컬에만 존재

### 커밋·푸시

- [완료] 커밋 `49cb04d` — 43 파일. `/ax` 전체 + `components/{Intro,Cursor,ClientEffects}.tsx` + `app/layout.tsx` + `.vercelignore` + 문서
  - 뺀 것: `public/ax/` 12M(GPT 이미지 4종·`mockup-*.png` 8종·`hero-field.png` — 코드 참조 0건 확인), `영업/` 레퍼런스 캡처 PNG 8M, `*.psd` `*.jpg` `*.log` `ax-*.png` `tsconfig.tsbuildinfo`
  - 넣은 것: 참조되는 `card-*.png` 8장(4.9M) + `영업/*.md` 16개
  - `main` 직접 커밋. **왜:** 프로덕션이 이 워킹트리에서 배포됐기 때문에 브랜치로 빼면 `main`이 라이브와 어긋난다
- [완료] 푸시 — `ralphxpdev-cell/tomob` `662dcba..49cb04d`
  - 1차 실패: `sungeunstar` 토큰으로 **403**(`Permission to ralphxpdev-cell/tomob.git denied`). 메모리 `reference_accounts`에 "ralphxpdev-cell PAT 미보유"로 이미 기록돼 있던 사례
  - 사령관이 PAT 발급 → `memory/reference_accounts.md`의 해당 항목 갱신 + remote URL에 임베드. 다음부터 `git push`만 하면 된다

### 문서

- [완료] `_PROJECT.md` 19,856자 → 5,606자. `## 완료된 작업` 65줄 누적 제거하고 `/save` 형식(지금 하는 것 1개 / 확정된 것 / 막고 있는 것 / 핵심 파일 / 대기 중 / 이슈)으로 재작성. 원문은 `work.md`·`work-archive/older.md`에 전문 보존
- [완료] `work.md` 신설 + 날짜별 재편, 2026-07-28 이전은 `work-archive/older.md`로 분리
- [완료] 세션 종료 시 `▶ 지금 하는 것`을 커밋 완료 → **`www.tomob.cloud/ax` 개통**으로 교체
  - **왜:** `www.tomob.cloud/`는 200인데 `/ax`는 404다. 오늘 만든 랜딩이 회사 도메인에 없어 영업에 못 쓴다. 실 라이브 도메인은 `junsungeuns-projects`에 없는 다른 Vercel 프로젝트에서 나가고 있다

### (이전 세션 항목 — 히어로 watson 구조 시절, 현재는 대체됨)

- [대체됨] 사령관 재검수 "레퍼런스랑 다르다 / 글씨가 구리다"로 히어로 타이포 교정. 자간(-0.025em)은 맞았는데 굵기(원본 200↔구현 500)와 줄 간격(원본 2~3배↔구현 1.06배)이 어긋나 있었다. 굵기 200 / 줄 간격 1.92배로 교정하고 워드마크 위 `SEOUL` 라벨 추가
- [대체됨] 사령관 "움직이는 GIF도 넘 별로" → 원인은 1.9초 하드컷. 6초 간격 + 0.8초 크로스페이드로 교체. watson도 `<video>` 0개이고 79×99 칩에서는 영상 내용이 안 읽힌다
- [대체됨] 각주 `(1)`이 앞 강조구에서 떨어져 다음 줄로 밀리는 결함 수정(`white-space: nowrap`)

---

## 2026-08-10

- [완료] 히어로 릴 8장(AI 생성 3D 기기 목업 — "AI가 만든 것 같다") 전면 폐기. `Reel.tsx` 삭제하고 `Wordmark.tsx`에서 검증된 기법을 확장한 풀블리드 WebGL 비주얼 `HeroVisual.tsx` 1개로 교체
- [완료] 1차 대체본(fBm 단일 레이어)이 "불꽃 같다" + "세로는 짧고 가로만 길다"로 반려. 레퍼런스를 lusion.co로 지정받아 **실제 GPU 유체 풀이**로 재작성(시뮬 128 / 염료 512). 높이 2.27:1 → 1.6:1
- [완료] 배경을 절차 생성 성운에서 **스타딜리버리 은하 정본**으로 교체. `createGalaxy` 생성식(count 34900·radius 10.5·branches 4·spin -0.3·randomness 0.77·power 2·inside `#c7bfdf`·outside `#e651e1`)과 가산합성·sizeAttenuation·모바일 18000 감축까지 그대로. three 의존성 없이 raw WebGL
- [완료] 유체가 별을 밀기만 해서는 육안으로 안 읽혀 염료를 은하 자신의 색으로 발광시키는 층 추가
- [완료] 메인 사이트 커스텀 커서를 `/ax`에서만 끔(`ax-reset.css`)
- [완료] "빛이 계속 흔들린다" 실측: 저은 뒤 15초까지 프레임 차이 1.43~1.49로 **전혀 감쇠 안 함**. 와도 강화 26이 감쇠보다 큰 에너지를 주입하고 있었다. 2.2로 낮추고 속도 감쇠 0.22→1.6 → idle 0 / 직후 0.76 / 3초 0.012 / 8초 0으로 수렴
- [완료] watson.la 실측 — `<video>` 0개, 한 페이지 2화면, "중간 영상"의 정체는 진술문 안 미디어 칩(79×99·135×99) 주기 교체. 워드마크 320.5px, 진술문 89.1px/lh 89.86/ls -2.23px
- [완료] 위 실측대로 히어로를 watson 구조로 교체(1스크린). `HeroStatement.tsx` 신설
- [완료] 명조 혼용은 사령관 명시 지시("혼용에서 써봐")로 tomob 폰트 규칙의 예외. 히어로 강조구에만 적용
- [완료] 히어로 실렌더 검수 결함 2건 수정: 한글 어절 중간 잘림(`word-break: keep-all`), 연분홍 PEACH 칩이 빈 판으로 보임(대비 확실한 캡처만 + 칩 확대 + `object-position 50% 38%`)
- [완료] **히어로 아래 Seed 밴드 통째로 제거(최종).** iframe 실측 174요청 64.34MB로 본체(10요청 0.17MB)의 380배. 첫 화면에 들어와 지연 로딩 무력화. 제거 후 37요청 3.44MB
- [완료] CTA 목적지를 카카오톡 오픈채팅 → **TOMOB Seed 접수 포털**로 교체 (`CONTACT` 상수 한 곳)
  - ⚠️ 2026-08-14: 이 CTA 목적지는 다시 페이지 내부 자가진단 폼(`#ax-pain`)으로 되돌렸다 — 다음 세션 로그 참조
- [완료] Seed 밴드 구현·조정 이력(진입 200px 전 마운트, 모바일 정적 캡처 대체, 크롭 128%→100% 되돌림, 오버레이 제거, 카피 2회 교체, iframe 내부 조작 허용)
- [완료] `.pill` 호버 텍스트 롤업 제거(사령관 판정). 중복 라벨 마크업 3곳 제거로 스크린리더 이중 낭독도 해소
- [완료] "원본은 빙글빙글 돈다" — 정본에서 은하 배치는 초기값이 아니라 **렌더 루프가 매 프레임 덮어쓰고** 있었다. `rotation.x=78°`·`z=0.62`·거리 `dist*2.1`·`rotation.y += dt*0.06`으로 교정. 회전 행렬도 three `makeRotationFromEuler` XYZ 식으로 교체
- [완료] "내가 설정한 별 색·모양이 아니다" — (1) 정본 렌더러가 ACESFilmic인데 톤매핑 없이 임의 밝기 3.4배를 곱하고 있었다 (2) PointsMaterial에 `map`이 없어 사각 점인데 없는 방사형 감쇠를 넣고 있었다 (3) 배경을 정본 `0x000000`으로. 전부 교정
- [완료] 검수: `tsc --noEmit` 통과, 헤드리스 1440×900 폴백 미발동·에러 0건. 평균 휘도 idle 24.57 → 직후 25.37 → 2.2초 24.55로 궤적 감쇠 실측

---

## 2026-08-07

- [완료] AGR 재작성본도 `엉망`으로 사령관 최종 판정. 완료본·승인 디자인으로 취급 금지, 재작업용 실패본 스냅샷으로만 보존
- [완료] 실패 원인이 "섹션 순서만 베끼고 실측을 안 한 것"으로 정리돼 agr.studio 전수 실측. 섹션 간격 321px, 타입스케일 90/63/61/26/25/16/14/13, 컬럼 1440 고정, 릴 타일 629×679·gap 12px, 프로젝트 이미지 708×681, 전 섹션 스크롤 리빌, FLOW 4열 계단식. `영업/AGR-실측-정본-2026-08-07.md` 저장
- [완료] 히어로 워드마크가 CSS 그라디언트가 아니라 **WebGL 캔버스(1440×178)** 임을 확인. 텍스트 알파 마스크 + 2단 도메인 워프 fBm 셰이더로 동일 기법 구현(`Wordmark.tsx`)
- [완료] `/ax` TSX·CSS를 실측값 기준 전면 재작성. 폰트는 Genesis 금지 규칙대로 Inter 대신 Geist + Pretendard
- [완료] 이미지를 GPT 추상 이미지 → **실제 결과물**로 교체. WORK는 뉴시즌 포폴 썸네일, 히어로 릴은 실제 제품 화면
- [완료] `globals.css` 전역 element 규칙이 `/ax`로 새는 문제를 `ax-reset.css`로 차단
- [완료] `collaboration/` 운영체계 구성 — 작업 보드·결정 로그·개인 worklog·인계 템플릿·AI 공통 규칙·텔레그램 명세
- [완료] `main` 협업 문서 변경을 텔레그램으로 보내는 GitHub Actions + 무의존성 Node 스크립트
- [완료] 검수: 프로덕션 빌드 통과, 1440 실렌더 전 섹션 확인. FLOW 4×360=1440, 계단 0/64/128/192, 바 25/50/75/100%, PLANS 708+708, CTA 61px/54.9px/-2.44px. 모바일 390px 동일 오리진 iframe 확인

---

## 2026-08-06

- [완료] AX 전환 검토 — 국내외 5개 수주 구조 비교, `한 업무·10영업일·실데이터 검증` 초기 상품 가설 정리
- [완료] 국내 한정 재조사: LG CNS·삼성SDS·SK AX·메가존클라우드 선별, 소형 검색 노출 업체 제외
- [완료] 범위 재지정으로 대기업 제외, 소규모 AX 8곳 Tranco 재검증. 달파를 핵심 벤치마크로 판정
- [완료] 검색 노출 중심 선별의 한계를 수정하고 실적 기반 재조사. 한국딥러닝·달파 직접 벤치마크, 인핸스·BHSN·액션파워는 성장 모델로 분리
- [완료] 사령관 지정 조슈아앤컴퍼니 기준 재조사. AIPM·PASSOME·Caliber·처음AX를 비교군으로 선별
- [완료] **AX 랜딩 기획 완료** — 채용 전 업무점검을 첫 전환 상품으로, Find→Build→Run, 증거 구축 로드맵, 섹션별 카피 (`영업/TOMOB-AX-랜딩페이지-기획안-2026-08-06.md`)
- [완료] 독립 `/ax` 랜딩 1차 구현. 프로덕션 빌드 + 1440/390 실렌더 검수
- [완료] 디자인 어워드 사이트 동일 조건 실캡처. ForAI·HappyOps·Webisoft·Pathways 후보 선정
- [완료] 사령관이 AGR Studio를 최종 시각 레퍼런스로 선택. 1440×16000 풀페이지 실캡처 후 적용안 분석
- [완료] `/ax`를 AGR식 흑백 에디토리얼로 전면 재디자인. 1차 Three.js 프로세스 장면은 레퍼런스와 다르다는 판정으로 폐기

---

## 날짜미상

- [완료] 가로 스크롤 8섹션 레이아웃
- [완료] 커스텀 커서 + 인트로 애니메이션
- [완료] Next.js 앱 구축 및 Vercel 배포
